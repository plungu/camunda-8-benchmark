<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.camunda.community</groupId>
    <artifactId>community-hub-release-parent</artifactId>
    <version>1.4.4</version>
    <relativePath />
  </parent>

  <artifactId>camunda-8-benchmark</artifactId>
  <version>0.0.1-SNAPSHOT</version>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0</url>
    </license>
  </licenses>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <license.header>com/mycila/maven/plugin/license/templates/APACHE-2.txt</license.header>
    <version.java>11</version.java>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.google.guava</groupId>
      <artifactId>guava</artifactId>
      <version>31.1-jre</version>
    </dependency>

<!--    <dependency>-->
<!--      <groupId>io.camunda.spring</groupId>-->
<!--      <artifactId>spring-boot-starter-camunda</artifactId>-->
<!--      <version>8.4.0</version>-->
<!--    </dependency>-->

    <dependency>
      <groupId>io.camunda</groupId>
      <artifactId>spring-boot-starter-camunda-sdk</artifactId>
      <version>8.7.1</version>
    </dependency>

    <dependency>
      <groupId>io.prometheus</groupId>
      <artifactId>simpleclient_httpserver</artifactId>
      <version>0.15.0</version>
    </dependency>

    <dependency>
      <groupId>com.typesafe</groupId>
      <artifactId>config</artifactId>
      <version>1.4.2</version>
    </dependency>

    <dependency>
      <groupId>io.dropwizard.metrics</groupId>
      <artifactId>metrics-core</artifactId>
      <version>4.2.25</version>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
      <version>3.3.2</version>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
      <version>1.12.5</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webflux</artifactId>
      <version>3.3.1</version>
    </dependency>
	<dependency>
	    <groupId>commons-lang</groupId>
	    <artifactId>commons-lang</artifactId>
	    <version>2.6</version>
	</dependency>
    <!--
        <dependency>
          <groupId>me.dinowernli</groupId>
          <artifactId>java-grpc-prometheus</artifactId>
          <version>0.5.0</version>
          <exclusions>
            <exclusion>
              <groupId>io.grpc</groupId>
              <artifactId>grpc-all</artifactId>
            </exclusion>
          </exclusions>
        </dependency>
        -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.9.3</version>
      <scope>test</scope>
    </dependency>

    <!-- JMeter’s Java Sampler API (where AbstractJavaSamplerClient lives) -->
    <dependency>
      <groupId>org.apache.jmeter</groupId>
      <artifactId>ApacheJMeter_java</artifactId>
      <version>5.5</version>
      <scope>provided</scope>
    </dependency>

    <!-- JMeter Core engine and test elements -->
    <dependency>
      <groupId>org.apache.jmeter</groupId>
      <artifactId>ApacheJMeter_core</artifactId>
      <version>5.5</version>
      <scope>provided</scope>
    </dependency>

    <!-- Jorphan: JMeter’s common utilities -->
    <dependency>
      <groupId>org.apache.jmeter</groupId>
      <artifactId>jorphan</artifactId>
      <version>5.5</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>com.mycila</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <version>3.0</version>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <configuration>
          <mainClass>org.camunda.community.benchmarks.BenchmarkApplication</mainClass>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>3.2.2</version>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <compilerArgs>
            <arg>-parameters</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <!-- 1) JMeter Maven Plugin -->
      <plugin>
        <groupId>com.lazerycode.jmeter</groupId>
        <artifactId>jmeter-maven-plugin</artifactId>
        <version>3.0.0</version>
        <executions>
          <execution>
            <id>jmeter-load</id>
            <goals>
              <goal>jmeter</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <!-- point at your JMeter test plans -->
          <testFilesDirectory>${basedir}/src/test/jmeter</testFilesDirectory>
          <testFilesIncluded>
            <jMeterTestFile>camunda-load.jmx</jMeterTestFile>
          </testFilesIncluded>

          <!-- download & use JMeter 5.5 -->
          <jmeterVersion>5.5</jmeterVersion>

          <!-- include the gRPC plugin so JMeter can speak Zeebe’s gRPC API -->
          <jmeterExtensions>
            <artifact>com.github.zalopay-oss:jmeter-grpc-plugin:1.3</artifact>
          </jmeterExtensions>

          <!-- pass host/port/processId/payload via Maven properties or env vars -->
          <propertiesUser>
            <gatewayHost>${env.ZEEBE_GATEWAY_HOST}</gatewayHost>
            <gatewayPort>${env.ZEEBE_GATEWAY_PORT}</gatewayPort>
            <bpmnProcessId>benchmark-process</bpmnProcessId>
            <payload>{"foo":"bar"}</payload>
          </propertiesUser>
        </configuration>
      </plugin>

    </plugins>
  </build>

  <!-- optional: a profile to isolate performance runs -->
  <profiles>
  <profile>
    <id>performance</id>
    <activation>
      <property>
        <name>perform</name>
        <value>true</value>
      </property>
    </activation>
    <!-- config above will only run when you invoke:
         mvn verify -Pperformance -Dperform=true  -->
  </profile>
  </profiles>

</project>
